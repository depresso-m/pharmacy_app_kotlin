<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Pharmacy_app" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="android:windowFullscreen">true</item>
        <item name="android:statusBarColor" tools:targerApi="l">@color/light_blue</item>
    </style>

    <style name="Theme.Pharmacy_app" parent="Base.Theme.Pharmacy_app" />
</resources>